# The app holds no user data: archives live in a folder of the user's choosing.
# Only machine-local things are ignored here.

# Speech models are large binaries, fetched by `npm run setup`.
/bin/models/
/bin/whisper-cli
/bin/whisper-cli.exe

node_modules/
*.log
.DS_Store
